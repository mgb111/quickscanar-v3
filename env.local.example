# Copy this file to .env.local and fill in your values

# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (Optional - for server-side operations)
# This is needed for file uploads and some API routes
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Site Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# File Upload Limits
MAX_FILE_SIZE_MB=100

# Note: For development, you can use the anon key for most operations
# The service role key is only needed for admin operations and file uploads
